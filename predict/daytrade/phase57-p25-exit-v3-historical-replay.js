import {assembleP253AutonomousEvidenceInputs} from './phase57-p25-3d-autonomous-evidence-evaluation.js';
import {runP253AKDynamicManagementSession,buildP253AKManagementRows} from './phase57-p25-3ak-dynamic-management-prospective.js';
import {buildP25DataDrivenExitAnalogPool} from './phase57-p25-data-driven-exit.js';
import {simulateP25ExitV3DualGate,P25_EXIT_V3_DUAL_GATE_POLICY_SHA256} from './phase57-p25-exit-v3-dual-gate.js';
import {P25_EXIT_V3_INDEPENDENT_PROTOCOL,P25_EXIT_V3_SAFETY} from './phase57-p25-exit-v3-independent-protocol.js';

export const P25_EXIT_V3_HISTORICAL_REPLAY_SAFETY=Object.freeze({...P25_EXIT_V3_SAFETY,mode:'DIAGNOSTIC_REPLAY_ONLY_NOT_OOS_NOT_PROMOTION_ELIGIBLE'});
const finite=v=>v!==null&&v!==undefined&&v!==''&&Number.isFinite(Number(v));
const mean=xs=>xs.length?xs.reduce((s,x)=>s+x,0)/xs.length:null;
const median=xs=>{if(!xs.length)return null;const a=[...xs].sort((x,y)=>x-y),m=Math.floor(a.length/2);return a.length%2?a[m]:(a[m-1]+a[m])/2;};
const keyOf=row=>`${String(row?.sessionDate??'')}|${String(row?.entryTimestamp??'')}|${String(row?.symbol??'').trim().toUpperCase()}`;
function profitFactor(xs){const gp=xs.filter(x=>x>0).reduce((s,x)=>s+x,0),gl=-xs.filter(x=>x<0).reduce((s,x)=>s+x,0);return gl>0?gp/gl:(gp>0?Infinity:null);}
function maxDrawdownPct(xs){let e=1,p=1,m=0;for(const r of xs){e*=1+r/100;p=Math.max(p,e);m=Math.max(m,(p-e)/p*100);}return m;}
function summarizeReturns(xs){const r=xs.map(Number).filter(Number.isFinite);let e=1;for(const x of r)e*=1+x/100;return Object.freeze({n:r.length,netReturnPct:(e-1)*100,meanNetReturnPct:mean(r),medianNetReturnPct:median(r),winRate:r.length?r.filter(x=>x>0).length/r.length:null,profitFactor:profitFactor(r),maxDrawdownPct:maxDrawdownPct(r)});}
function summarizePairs(rows){const f=rows.map(x=>x.fixed.netReturnPct).filter(finite).map(Number),v=rows.map(x=>x.v3.netReturnPct).filter(finite).map(Number),d=rows.map(x=>x.deltaNetReturnPct).filter(finite).map(Number);return Object.freeze({pairedCount:rows.length,fixed:summarizeReturns(f),v3:summarizeReturns(v),delta:Object.freeze({meanNetReturnPct:mean(d),medianNetReturnPct:median(d),v3BetterCount:d.filter(x=>x>0).length,v3WorseCount:d.filter(x=>x<0).length,equalCount:d.filter(x=>x===0).length,meanBarsHeld:mean(rows.map(x=>x.v3.barsHeld).filter(finite).map(Number)),meanMfePct:mean(rows.map(x=>x.v3.mfePct).filter(finite).map(Number)),meanMaePct:mean(rows.map(x=>x.v3.maePct).filter(finite).map(Number)),meanGivebackPct:mean(rows.map(x=>x.v3.givebackPct).filter(finite).map(Number)),meanCaptureRatio:mean(rows.map(x=>x.v3.captureRatio).filter(finite).map(Number))})});}

export function classifyP25ExitV3ReplayDate(sessionDate){const d=String(sessionDate??'');if(!/^\d{4}-\d{2}-\d{2}$/.test(d))throw new Error('P25 EXIT v3 replay requires YYYY-MM-DD session date');if(d<=P25_EXIT_V3_INDEPENDENT_PROTOCOL.developmentCutoff)return Object.freeze({bucket:'DEVELOPMENT_REPLAY',formalOos:false,diagnosticOnly:true,promotionEligible:false});if(d>='2026-08-19'&&d<='2026-08-25')return Object.freeze({bucket:'LEGACY27_DIAGNOSTIC_REPLAY',formalOos:false,diagnosticOnly:true,promotionEligible:false});return Object.freeze({bucket:'OTHER_HISTORICAL_REPLAY',formalOos:false,diagnosticOnly:true,promotionEligible:false});}

export function runP25ExitV3HistoricalReplay({historyPack,captureArtifacts=[],sessionIntegrityLedger,lineageManifest,sessionDate,scorePrefix=null,roundTripCostPct=0.05}={}){
  const classification=classifyP25ExitV3ReplayDate(sessionDate);
  const assembled=assembleP253AutonomousEvidenceInputs({historyPack,captureArtifacts,sessionIntegrityLedger,lineageManifest});
  const selected=assembled.sessionInputs.filter(x=>String(x?.universeRecord?.sessionDate??'')===String(sessionDate));if(selected.length!==1)throw new Error('P25 EXIT v3 historical replay requires exactly one complete immutable captured session');
  const analogPool=buildP25DataDrivenExitAnalogPool({historicalSessions:assembled.historicalSessions}).filter(x=>String(x.sessionDate)<=P25_EXIT_V3_INDEPENDENT_PROTOCOL.developmentCutoff);if(!analogPool.length)throw new Error('P25 EXIT v3 historical replay: frozen causal analog pool empty');if(analogPool.some(x=>String(x.sessionDate)>P25_EXIT_V3_INDEPENDENT_PROTOCOL.developmentCutoff))throw new Error('P25 EXIT v3 historical replay: post-cutoff analog contamination');
  const input=selected[0];const base=runP253AKDynamicManagementSession({universeRecord:input.universeRecord,historicalSessions:assembled.historicalSessions,sessionBarsBySymbol:input.sessionBarsBySymbol??{},...(typeof scorePrefix==='function'?{scorePrefix}:{})});
  const frozenTrades=base?.replay?.ledger?.frozenTrades??[],managed=buildP253AKManagementRows({frozenTrades,sessionBarsBySymbol:input.sessionBarsBySymbol??{}}),rowByKey=new Map(managed.rows.map(r=>[keyOf(r),r])),fixedByKey=new Map((base?.fixedOutcomes?.resolvedTrades??[]).map(r=>[keyOf(r),r]));const pairs=[];let dynamic50FrozenCount=0;
  for(const trade of frozenTrades){if(!(trade?.variantMemberships??[]).includes('DYNAMIC_50'))continue;dynamic50FrozenCount++;const key=keyOf(trade),row=rowByKey.get(key),fixedOutcome=fixedByKey.get(key);if(!row||!fixedOutcome)continue;const fixedNet=Number(fixedOutcome.netReturnPct??fixedOutcome.afterCostReturnPct??fixedOutcome.returnPct);if(!Number.isFinite(fixedNet))throw new Error(`P25 EXIT v3 historical replay: fixed net missing for ${key}`);const v3=simulateP25ExitV3DualGate({row,analogPool,roundTripCostPct});if(v3.policySha256!==P25_EXIT_V3_DUAL_GATE_POLICY_SHA256)throw new Error('P25 EXIT v3 historical replay: runtime policy hash mismatch');pairs.push(Object.freeze({key,sessionDate:trade.sessionDate,symbol:trade.symbol,variantMemberships:Object.freeze([...(trade.variantMemberships??[])]),fixed:Object.freeze({...fixedOutcome,netReturnPct:fixedNet}),v3,deltaNetReturnPct:v3.netReturnPct-fixedNet}));}
  pairs.sort((a,b)=>a.key.localeCompare(b.key));if(pairs.some(p=>!p.variantMemberships.includes('DYNAMIC_50')))throw new Error('P25 EXIT v3 historical replay: non-DYNAMIC_50 pair detected');
  return Object.freeze({phase:'57.p25.exit-v3.historical-replay',status:'P25_EXIT_V3_HISTORICAL_REPLAY_EVALUATED',classification,policySha256:P25_EXIT_V3_DUAL_GATE_POLICY_SHA256,sessionDate:String(sessionDate),lineageManifestHeadSha256:assembled.lineageManifestHeadSha256,analogPoolCount:analogPool.length,dynamic50FrozenCount,pairedCount:pairs.length,managementBlockedCount:managed.blocked.length,pairs:Object.freeze(pairs),summary:summarizePairs(pairs),methodology:Object.freeze({exactDynamic50Only:true,sameFrozenEntryAsFixed:true,fixedBaselineUntouched:true,developmentCutoff:P25_EXIT_V3_INDEPENDENT_PROTOCOL.developmentCutoff,legacy27UsedForThresholdTuning:false,legacy27UsedForFeatureSelection:false,legacy27UsedForHorizonSelection:false,legacy27UsedForCoefficientSelection:false,legacy27UsedForCandidateSelection:false,replayOutcomeUsedForFitting:false,resultBasedRetuning:false,formalOosEvidence:false,promotionEligible:false,freshHoldoutConsumed:false}),safety:P25_EXIT_V3_HISTORICAL_REPLAY_SAFETY});
}
export default {classifyP25ExitV3ReplayDate,runP25ExitV3HistoricalReplay,P25_EXIT_V3_HISTORICAL_REPLAY_SAFETY};
