name: Phase57 P25 Legacy Dynamic EXIT Persistence (Retired)

# RETIRED from the active four-track validation on 2026-08-28.
# Historical artifacts on automation/p25-dynamic-management-data remain immutable.
# Active Track B is now Dynamic 5m Selection -> Frozen Entry -> EXIT v3.
# It is represented by the marketwide 5m measurement and four-track durable evidence pipelines.
# No schedule/push trigger remains here so the rejected legacy Dynamic HOLD/EXIT is not
# recomputed as an active research track.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy B retirement boundary
        run: |
          echo 'Legacy Fixed vs Dynamic HOLD/EXIT Track B is retired from active parallel validation.'
          echo 'Historical automation/p25-dynamic-management-data evidence is preserved and not rewritten.'
          echo 'Active B = Dynamic 5m Selection -> Frozen Entry -> EXIT v3.'
          echo 'No broker/RSS/Excel/order/live/paper execution, promotion, production update, or fresh holdout consumption.'
