import assert from 'node:assert/strict';
import {fitHierLevel,scoreHierLevel,blendHierScores,walkForwardHierarchical,P23_32_POLICY} from '../daytrade/phase57-hierarchical-dynamic-risk.js';
const state=x=>({structureCoherence:x,directionalPressure:x+.01,wickControl:x-.01,extensionHealth:x+.02,spaceToObstacle:x-.02,setupGeometryFit:x+.015,qualityScore:x-.015});
const hist=[];for(let i=0;i<40;i++)hist.push({actual:i<12?1:0,velocity:state((i<12?.7:.3)+(i%5)*.004)});const m=fitHierLevel(hist);assert.equal(m.ready,true);const hi=scoreHierLevel({velocity:state(.85)},m),lo=scoreHierLevel({velocity:state(.2)},m);assert.ok(Number.isFinite(hi)&&Number.isFinite(lo));assert.ok(hi>lo);
assert.equal(blendHierScores(1,0,30),.5);assert.equal(blendHierScores(null,.25,0),.25);assert.equal(blendHierScores(.25,null,40),.25);
const rows=[];for(let d=1;d<=45;d++){const date=`2026-06-${String(((d-1)%30)+1).padStart(2,'0')}`,month=d<=30?'06':'07',day=d<=30?d:d-30,session=`2026-${month}-${String(day).padStart(2,'0')}`,bad=d%3===0;for(const setup of ['A','B'])rows.push({symbol:'X',setup,direction:'UP',sessionDate:session,timestamp:`${session}T01:00:00.000Z`,fullyRealizedAt:`${session}T01:20:00.000Z`,velocity:state((bad?.75:.25)+(setup==='B'?.02:0)),actual:bad?1:0,nextDirectionalReturnPct:bad?-.4:.2});}const out=walkForwardHierarchical(rows);assert.ok(out.length>0);for(const r of out){assert.ok(Number.isFinite(r.riskScore));assert.ok(r.directionHistoryCount>=r.exactHistoryCount);}
for(const k of ['thresholdSearchAllowed','selectionAllowed','exitPolicyChangeAllowed','entryRetuningAllowed','symbolFilteringAllowed','freshHoldoutConsumed'])assert.equal(P23_32_POLICY[k],false);assert.equal(P23_32_POLICY.priorSessionsOnly,true);console.log('P23.32 hierarchical dynamic risk invariants: OK');
