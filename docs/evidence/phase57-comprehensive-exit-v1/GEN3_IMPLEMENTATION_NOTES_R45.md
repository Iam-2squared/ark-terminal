# Gen3 R45 implementation checkpoint — not a performance result

Saved at JST: 2026-09-26T23:09:02+09:00.
Basis research HEAD: `98345947c14ffd40c6283ce4e9be33139989b609`.
Implementation branch: `research/phase57-gen3-r45-ci-20260926-implementation` (no PR).
Repository: `Iam-2squared/ark-terminal`; Draft PR #587 remains unmerged.

## Authorization and immutable scope

The user subsequently instructed: 「もうworkじゃなくてここで進めて。action起動までやって」. This authorizes launch ONLY after implementation, support verification and the exact-source required CI succeed. It supersedes the earlier operational stop-before-launch instruction, not the R45 scientific protocol.

The original R45 JSON remains byte-identical: SHA-256 `e7b38e7e6aaf909926852467152fffef2532f58f960a95e6f2d18efd66f5d1b5`. Four registered structural policies; one HGB/Pattern187 specification; three heads; two frozen Entry arms; four folds; exactly 24 planned fits. No new candidate, threshold, model, label or Completion Gate. The original JSON's currentWork/markerMustNotExistThisWork fields remain historical records of the previous precommit Work; the new explicit user authorization is recorded here and in the separate future launch marker.

Entry Dual Freeze `4878a1cc53430e816261dea0fb16aeb53b3c238d` is untouched. R36/R39 and R41/R43 remain NO_SELECTION. No Capital comparison is authorized before an eventual audited SELECT/EXIT Freeze.

## Current implementation

New R45 modules separate current-only runtime, causal facts, isolated sampled executable-utility labels, data assembly, preflight, support-only verification, finite runner and deferred evaluator. No frozen earlier source file was edited. Runtime accepts only the exact current fact envelope, registered candidate, previous state and three finite current scores. It does not import training data, raw future paths, estimators or an evaluator.

The data path verifies immutable R35 A/B bytes and all pinned Development input hashes; preserves 656,247 rows and their Entry/arm/epoch identity; and uses 21 categories, 106 numeric columns and the exact 187 Pattern columns. Label availability/maturity/sample prices are training-only. Sampled-label support does not require complete unsampled future bars and does not claim complete future-path observation.

Technical edge interpretations are fixed before performance: the first valid certified peak primes peak memory; strict increases, not equal-price ties, are newPeak events. Gaps/stale/unknown reset relevant observed histories. A new certified peak clears protection count/probation and suppresses protection for that endpoint; deterioration still takes priority. Current raw-derived fact values are retained in float64 for action semantics; the model matrix remains the frozen float32. This is transport/implementation, not a new search dimension.

Deterioration and neutral-deterioration share the specified D confirmation counter; protection has its separate counter. Actual recovery events grant the fixed current-and-next-fresh protection probation, never a permanent C veto. Missing ordinary OPEN leaves no queued order; each new endpoint must independently satisfy the current rule. Terminal 925 forces only exact auction930, with missing auction unresolved. DROP, negative PnL, one signal loss or uncertified giveback alone cannot cause a model EXIT.

The learner will persist actual train/score index arrays and training weights for every model, source/input identities, label masks/maturity/reasons, exact current facts, one OOF prediction set, and full authority/state traces plus A/B ledgers for all four candidates and the nonselectable terminal diagnostic. This improves audit observability without changing targets or decisions. It produces no aggregate scorecard or selection; its terminal status is AWAITING_POST_RUN_AUDIT.

## Verification already performed locally

- Synthetic R45 tests: 87 PASS.
- Combined R45 plus unchanged runtime/cache/capture boundaries: 142 PASS.
- Support-only data assembly: 656,247 rows; no fit, policy replay or candidate performance.
- All 24 fold/Entry/head support slices satisfy the immutable gate.
- Minimum train support across slices: 11,513 rows; minimum class support 5,374; minimum unique Opportunities 178; minimum sessions 22.
- Independent target/reference/mask/maturity reconstruction: 656,247 rows / 1,968,741 target cells; mismatches 0. This second implementation does not call the R45 target producer.

Local dependency versions are not represented as the required pinned CI environment. The required GitHub CI has NOT yet passed at this checkpoint. Local observations are preliminary implementation evidence only, not launch clearance.

## Required CI and launch plan

Create `Phase57 Gen3 Contract R45` only on the isolated CI branch. It installs the exact pinned Python/numerical dependencies, runs the combined synthetic suite, rebuilds support-only data, independently verifies all label cells, and saves the exact source/CI/support receipt. No Gen3 Development models or performance replay run in that CI.

After SUCCESS, publish the tested commit and a documentation-only readiness closure to the research branch by a non-force fast-forward, after rechecking its actual HEAD. Then create a dedicated `research/phase57-gen3-r45-launch-*` branch and the single separate GEN3_LAUNCH_R45.json marker. This launch-only branch avoids the existing PR's cumulative historical-workflow triggers. It is an operational isolation detail; candidate/data/model/gate semantics remain unchanged.

The marker binds executionSha, protocol hash, successful contractRunId, trigger branch, researchHead, four candidates, 24 fits and explicit user authorization. The learning workflow checks out that exact tested SHA, verifies actual source and CI receipt, and rejects any other run of the finite workflow, including previous failures. It rechecks trigger/research heads immediately before fitting. No automatic retry, resume or refit is authorized.

## Failure history and limitations retained

The local runtime cannot resolve external DNS for a git clone. The ordinary connected GitHub tools and a successful source-only isolated Action were used to obtain the permitted sources: run `36245904710`, artifact `10907039128`, ZIP SHA-256 `5de08e1c750272d76664ec67b9315e3ab2b79c5cb9dca30c88a9ec80b55fc500`.

Two local support starts stopped before output because the local source package lacked the existing Pattern names registry. The registry was reconstructed from the frozen feature audit and checked against its exact recorded hash; no repository registry was changed. An initial synthetic AST isolation test incorrectly matched the substring data in dataclasses; the test was narrowed to actual prohibited module families. Final local tests passed. No Gen3 fit or performance inspection occurred in any of these attempts.

The historical bar-end knownAt proxy is not proof of live publication latency. Incomplete owned prefixes still cannot certify giveback. Positive label support is not evidence of predictive quality, profitability or Completion Gate success.

## Safety, exposure and next action

Gen3 Development fit=0; performance-bearing replay=0; candidate performance inspected=0 at this checkpoint. Provider acquisition=0; protected partition opens=0; all Safety9 false. No main merge, force push, order submission, live/paper/production, broker/Excel/RSS write, or automatic promotion.

Next: save workflows, pass required exact-source CI, publish readiness, then create the explicitly authorized launch marker once. Do not report launch until the actual GitHub run and execution identity have been verified. Do not move research/trigger HEAD while the pre-fit guard is pending.
