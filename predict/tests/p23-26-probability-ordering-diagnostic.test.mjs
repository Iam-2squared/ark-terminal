import assert from 'node:assert/strict';import {estimateAtFixedK,orderingScore,quantileBands,P23_26_POLICY} from '../daytrade/phase57-probability-ordering-diagnostic.js';
assert.deepEqual(P23_26_POLICY.diagnosticNeighborCounts,[10,20,40]);assert.equal(P23_26_POLICY.thresholdSearchAllowed,false);assert.equal(P23_26_POLICY.selectionAllowed,false);
const mk=(i,bad)=>({setup:'BREAKOUT_CONTINUATION_UP',direction:'UP',timestamp:new Date(100000+i*10000).toISOString(),fullyRealizedAt:new Date(i*10000).toISOString(),state:{structureCoherence:i/20,directionalPressure:.5,wickControl:.5,extensionHealth:.5,spaceToObstacle:.5,setupGeometryFit:.5,qualityScore:.8},invalidated:bad,nextDirectionalReturnPct:bad?-.5:.3});const pool=Array.from({length:12},(_,i)=>mk(i,i%3===0));const q={...mk(20,false),timestamp:new Date(9999999).toISOString()};const e=estimateAtFixedK(q,pool,10);assert.equal(e.ready,true);assert.equal(e.count,10);assert.ok(e.p>0&&e.p<1);assert.equal(estimateAtFixedK(q,pool,20).ready,false);const rows=[{p:.1,actual:0,nextDirectionalReturnPct:.2,meanDistance:.1},{p:.2,actual:0,nextDirectionalReturnPct:.1,meanDistance:.2},{p:.8,actual:1,nextDirectionalReturnPct:-.4,meanDistance:.1},{p:.9,actual:1,nextDirectionalReturnPct:-.5,meanDistance:.2}];assert.equal(orderingScore(rows,'p'),1);assert.equal(quantileBands(rows,'p',4).length,4);console.log('p23.26 diagnostic invariants ok');