# Phase57 G7 R2 — pre-distribution protocol

Recorded 2026-09-22 JST. Start HEAD: eb0217c20ebf5b43080bc988934845838569453e. User instruction: 完成させて, after the proposal to finish a separate Development review package. Scope is the review apparatus only. Original G7 v1 remains FAIL; State v2 is NOT_FROZEN. No market rule or numeric threshold changes.

## Source and input-only inventory

Use only the preserved G Development artifact (SHA256 cc921c476af1079344975daa78e89fe03ee0dfc20ee62333170199178ade795e), measurement manifest 4e11b8eb576dcdd0552f2461c699f57bdabc1db37d8e4c2be9a389a80748d6d1. Original admin ZIP SHA256 15e389cbf3638e89e5555e934fe73152b3d4782b5304d4348372ea407d3d932b defines excluded Opportunities. Reference.py SHA256 e57d41b1a9472fb0ed254895d956623a438557f540443c0bee14a6db8f2d8d3d is unchanged. Repaired display module SHA256 57f8133a8cc005e10e3e67bf0fa5190276a69885e57237d25a614e999906d36e is the source, with separate presentation/masking adapter for R2.

Input-only inventory before selection: 2,155 source Opportunities; exclude all 36 old actual/control-source Opportunities, leaving 2,119. Scale counts: AVAILABLE 978, SCALE_INSUFFICIENT 1,100, PREVIOUS_CONTEXT_UNAVAILABLE 41. No new PRICE_BASIS_UNVERIFIED or SCALE_ZERO Opportunity remains. Do not reuse the old rare examples as independent evidence. Those two absent cells are explicitly unrepresented in the new real sample and covered only by synthetic contract fixtures. Two released real slots are assigned before selection to NONE and latest5-partial-current-observed. This is not an OOS/Fresh study.

## Deterministic actual sample

Seed: phase57-g7-r2-20260922-causal-endtime-v1.
36 unique new Opportunities, at most one checkpoint per Opportunity. Enumerate checkpoints using the frozen selector-time/active-minute grid. Never use futureStatus, lateConfirmedPivots, future Structure, returns, PnL or Entry/EXIT results to screen or rank.
Quotas, evaluated with freshly computed CLOSED_PREFIX NOW: PREVIOUS_CONTEXT_UNAVAILABLE/current observed 1; current missing 3; latest5 partial/current observed/Scale AVAILABLE 4; SCALE_INSUFFICIENT/current observed 3; latest5 complete/Scale AVAILABLE/no active Structure/pivot0,1,2,3 each 3; latest5 complete/Scale AVAILABLE/DEFINED NONE 4; active RANGE, UP, DOWN with latest5 complete each 3. Rare statuses first, then missing/partial/Scale-limited, then pivot0..3, NONE, RANGE, UP, DOWN. A case fits only its assigned stratum; preserve all observation facts.
Within each quota greedily prefer least-used fixed time band then least-used fixed selector-price band, then SHA256(seed|Opportunity|checkpoint) and identity. Fixed time bands: before 10:00; through 11:30; before 14:00; remaining session. Price bins: <500, [500,1000), [1000,3000), [3000,10000), >=10000. No redistribution after examining reviewer results. Any further unavailable quota is a STOP, not silent substitution.

## Controls and domains

Eight intended defect targets are retained: wrong active Structure; false COMPLETE; post-t pivot confirmation; numeric unavailable Scale; evaluated NONE mislabeled NOT_APPLICABLE; invented FORMING; omitted required concurrent reason; omitted concurrent censor cause. Use unique synthetic price paths, not altered duplicates of actual cases. Fix each intended defect and witness predicate before distribution. Seven targets are NOW-contract fixtures. The censor target belongs to a separately headed EVALUATOR_ONLY synthetic clock/observation contract, never to the causal market card.
Add two correct EVALUATOR_ONLY synthetic fixtures so merely noticing an evaluator card is not the intended detection. Total reviewer budget: 36 actual + 8 fault-injected fixtures + 2 correct evaluator fixtures = 46. Correct evaluator fixtures are not counted in the 36-case kappa denominator or in the 8-defect denominator. All three evaluator cards explicitly use synthetic schedule/observation facts, no future market prices or PnL. They test the separate future-schema contract, not NOW. Their correct handling is separately reported.

## Reviewer masking and independence

Two fresh sessions, neither authoring nor differential Freeze reviewer; no shared responses. The identical package has two sequential passes. Pass 1 contains only causal input/witness tables and plots, fixed rule brief, no proposed axis labels. Reviewer writes and hashes PASS1.csv before opening pass 2. Pass 2 contains proposed outputs, not the admin truth, and is opened only after PASS1 is saved. Supply a delayed-release pass-2 archive plus local helper; user action once per reviewer. Because both stages are delivered in one package, this is procedural sequential masking, not cryptographically enforced access control. Record attestation and both files; do not claim stronger blinding.
Reviewers use exact numeric witnesses and the supplied definitions, not whole-day appearance as latest5 Direction. They retain separate input limitations and semantic adequacy concerns. Honest missingness is not a representation defect. If a case cannot be inspected, mark INDETERMINATE, never invent completion. No external symbol search or other reviewer results.

## Response and scoring

PASS1 records expected Direction status/value, Structure status/value, Phase status/value; evaluator cases use NOT_APPLICABLE for NOW axes and expected evaluator censor flags. PASS2 RESPONSE.csv: caseId, representationDefect YES/NO/INDETERMINATE, defectCodes (pipe separated closed enum), inputLimitationCodes, semanticAdequacyConcern NONE/NONBLOCKING/BLOCKING/INDETERMINATE, rationale. ALL 46 IDs exactly once in specified order; reject duplicates, unknown codes, contradictions and empty rationale. Preserve first-pass file/hash and reviewer independence/ordering attestation.
Defect codes: DIRECTION_RULE_MISMATCH, STRUCTURE_RULE_MISMATCH, PHASE_RULE_MISMATCH, OBSERVATION_STATUS_MISMATCH, FUTURE_PIVOT_IN_NOW, FUTURE_BAR_IN_NOW, FORBIDDEN_FIELD_IN_NOW, SCALE_STATUS_VALUE_MISMATCH, NEGATIVE_FINDING_STATUS_MISMATCH, UNDEFINED_STATE_VALUE, REQUIRED_REASON_OMITTED, CENSOR_CAUSE_OMITTED, WITNESS_INCONSISTENCY, OTHER_EXPLICIT_RULE_DEFECT. Specific control credit requires the pinned target code and a rationale addressing its visible predicate, not any generic issue. The scorer produces only a mechanical result; author adjudication of rationales is explicit and nonblind.
Both reviewers must detect 8/8 intended controls. Actual binary representation-defect kappa >=0.60. Only if kappa is mathematically undefined use raw agreement >=85%, still requiring both 8/8. INDETERMINATE is reported and prevents finalization until adjudicated; it is not silently NO or an exclusion. Any actual future leakage is a hard fail. Blocking semantic gaps require adjudication; do not retune v2.0 on this set. First-pass accuracy and semantic concerns reported separately, no profit claim. Correlated LLM error remains a limitation.

## Before delivery QA

Recompute selected cards from exact reference.py snapshot; fail if reference_at is called. Verify barEnd/knownAt cutoffs, all local/origin pivot confirmations, prior Scale date, full source manifest, cohort membership and old-Opportunity exclusion. Actual post-t suffix replacement/removal must leave witness JSON, HTML and PNG hashes unchanged. Repeat in reverse processing order. Validate all synthetic base fixtures before mutation; single intended mutation stored only in admin truth; correct evaluator fixtures remain unchanged. Disallow identical normalized actual/control paths. Verify stage-1 has no proposed axis labels, source symbol/Opportunity IDs, control markers or future market fields; stage-2 includes no admin key. Exact payload/rule/module/producer/scorer hashes recorded. Render and inspect booklet pages for clipping. Review PASS is not asserted by package QA.

## STOP

Deliver completed reviewer ZIP and prompt, plus a separately labeled operator evidence archive. Save protocol/pins/QA to PR #587 branch only. No automatic external reviews, model fitting, full 77,214-row State v2 generation, State v2 Freeze, Signal, BUY/WAIT, Entry, EXIT, main merge, new market-provider acquisition or protected-data opening. Safety9 remain false.
