import assert from 'node:assert/strict';import {P23_25_POLICY,probabilityStateFrom,estimateSetupInvalidationProbability,calibrateSetupInvalidationProbability,summarizeProbability} from '../daytrade/phase57-setup-specific-invalidation-probability.js';
const visual={components:{structureCoherence:.7,directionalPressure:.6,wickControl:.5,extensionHealth:.8,spaceToObstacle:.7,setupGeometryFit:.9}};const state=probabilityStateFrom({visual,qualityScore:.8});assert.equal(state.qualityScore,.8);const pool=Array.from({length:25},(_,i)=>({setup:'BREAKOUT_CONTINUATION_UP',direction:'UP',timestamp:`2026-01-${String(i+1).padStart(2,'0')}T00:00:00.000Z`,fullyRealizedAt:`2026-01-${String(i+1).padStart(2,'0')}T00:15:00.000Z`,state,invalidated:i%5===0,nextDirectionalReturnPct:i%5===0?-.5:.2}));const q={setup:'BREAKOUT_CONTINUATION_UP',direction:'UP',timestamp:'2026-02-01T00:00:00.000Z',state};const r=estimateSetupInvalidationProbability(q,pool);assert.equal(r.ready,true);assert.ok(r.pInvalidation>0&&r.pInvalidation<1);const early=estimateSetupInvalidationProbability({...q,timestamp:'2026-01-10T00:00:00.000Z'},pool);assert.equal(early.ready,false);const hist=pool.map((x,i)=>({...x,rawP:.2,invalidated:i%5===0}));const cal=calibrateSetupInvalidationProbability(.2,q,hist);assert.equal(cal.ready,true);const s=summarizeProbability([{p:.2,actual:0,nextDirectionalReturnPct:.1},{p:.8,actual:1,nextDirectionalReturnPct:-.4}]);assert.equal(s.n,2);assert.ok(s.brier>=0&&s.brier<=1);assert.equal(P23_25_POLICY.thresholdSearchAllowed,false);assert.equal(P23_25_POLICY.outcomeTuningAllowed,false);console.log('P23.25 invariants ok');
