import fs from 'node:fs';import path from 'node:path';import {fileURLToPath,pathToFileURL} from 'node:url';
import {P23_10B_FRESH_HOLDOUT_SYMBOLS,P23_10B_FRESH_HOLDOUT_POLICY,PHASE57_P23_10B_SAFETY} from './phase57-fresh-persistence-holdout.js';import {simulateFrozenRatchetExit} from './phase57-frozen-ratchet-exit.js';
const SHARD_INDEX=Number(process.env.PHASE57_SHARD_INDEX??0),SHARD_COUNT=3;if(!Number.isInteger(SHARD_INDEX)||SHARD_INDEX<0||SHARD_INDEX>=3)throw new Error('bad shard');if(P23_10B_FRESH_HOLDOUT_SYMBOLS.length!==30)throw new Error('holdout must remain 30');const size=10,symbols=P23_10B_FRESH_HOLDOUT_SYMBOLS.slice(SHARD_INDEX*size,(SHARD_INDEX+1)*size),SHARD_NUMBER=SHARD_INDEX+1;
for(const k of ['executionAllowed','brokerWriteAllowed','excelOrderWriteAllowed','rssOrderFunctionAllowed','liveTradingAllowed','paperTradingAllowed','automaticPromotionAllowed','productionUpdateAllowed','overnightHoldingAllowed'])if(PHASE57_P23_10B_SAFETY[k]!==false)throw new Error(`${k} must remain false`);
process.env.PHASE57_SCOPE='COMBINED';const here=path.dirname(fileURLToPath(import.meta.url)),basePath=path.join(here,'run-phase57-real-nested-state-machine.mjs'),runtimePath=path.join(here,`.phase57-p23-10b-holdout-${SHARD_INDEX}-runtime.mjs`),dumpPath=path.resolve(`artifacts/phase57-p23-10b-holdout-exit-rows-${SHARD_NUMBER}.json`);let src=fs.readFileSync(basePath,'utf8');const originalUniverse="const universe = ['7203.T', '6758.T', '9984.T', '8306.T', '8035.T'];",originalFetch="const fetchedBarsBySymbol = new Map(await Promise.all(symbols.map(async symbol => [symbol, await fetchBars(symbol)])));",sortMarker="exitRows.sort((a, b) => a.featureCutoff.localeCompare(b.featureCutoff));";for(const [m,l] of [[originalUniverse,'universe'],[originalFetch,'fetch'],[sortMarker,'sort']])if(!src.includes(m))throw new Error(`patch marker missing ${l}`);src=src.replace(originalUniverse,`const universe = ${JSON.stringify(symbols)};`);src=src.replace(originalFetch,`const fetchedPairs=[];for(let start=0;start<symbols.length;start+=5){const batch=symbols.slice(start,start+5);fetchedPairs.push(...await Promise.all(batch.map(async symbol=>[symbol,await fetchBars(symbol)])));if(start+5<symbols.length)await sleep(1000);}const fetchedBarsBySymbol=new Map(fetchedPairs);`);src=src.replace(sortMarker,`${sortMarker}\nfs.mkdirSync('artifacts',{recursive:true});fs.writeFileSync(${JSON.stringify(dumpPath)},JSON.stringify(exitRows));`);fs.writeFileSync(runtimePath,src);try{await import(`${pathToFileURL(runtimePath).href}?v=${Date.now()}`);}finally{fs.rmSync(runtimePath,{force:true});}
const baseArtifact=path.resolve('artifacts/phase57-real-nested-state-machine-COMBINED.json');if(!fs.existsSync(baseArtifact)||!fs.existsSync(dumpPath))throw new Error('holdout runtime artifacts missing');const base=JSON.parse(fs.readFileSync(baseArtifact,'utf8')),exitRows=JSON.parse(fs.readFileSync(dumpPath,'utf8'));fs.rmSync(dumpPath,{force:true});const nested=base.nestedStateMachine;if(!nested?.stateMachine?.foldResults)throw new Error('nested fold results missing');
function foldFor(row){const d=String(row.sessionDate);return nested.stateMachine.foldResults.find(f=>f?.selectedCandidateId&&d>=String(f.testStart)&&d<=String(f.testEnd))??null;}const matched=[];for(const row of exitRows){const fold=foldFor(row);if(!fold)continue;const ratchet=simulateFrozenRatchetExit({...row,frozenEntry:true});if(!ratchet)continue;matched.push(Object.freeze({symbol:String(row.symbol),sessionDate:String(row.sessionDate),featureCutoff:new Date(row.featureCutoff).toISOString(),direction:Number(row.signalDirection)===1?'LONG':'SHORT',baseSignalConfidence:Number(row.baseSignalConfidence),baseHorizonBars:Number(row.baseHorizonBars),outerFold:Number(fold.fold),ratchet:Object.freeze({outcomeAt:ratchet.outcomeAt,barsHeld:ratchet.barsHeld,grossReturnPct:ratchet.grossReturnPct,netReturnPct:ratchet.netReturnPct,mfePct:ratchet.mfePct,maePct:ratchet.maePct})}));}matched.sort((a,b)=>a.featureCutoff.localeCompare(b.featureCutoff)||a.symbol.localeCompare(b.symbol));if(matched.length!==Number(nested.matchedOuterTestRowCount??0))throw new Error(`matched row reconciliation failed ${matched.length} != ${nested.matchedOuterTestRowCount}`);
const rule=P23_10B_FRESH_HOLDOUT_POLICY.persistenceRule,lookbackMs=Number(rule.lookbackMinutes)*60000,history=new Map(),activeUntil=new Map(),accepted=[],skipped={insufficientPersistence:0,activeSameSymbol:0};for(const row of matched){const t=Date.parse(row.featureCutoff),key=`${row.symbol}|${row.sessionDate}`,hist=(history.get(key)??[]).filter(x=>t-x.t<=lookbackMs);hist.push({t,direction:row.direction});history.set(key,hist);const count=hist.filter(x=>x.direction===row.direction).length;if(count<Number(rule.requiredSameDirectionSignals)){skipped.insufficientPersistence++;continue;}const active=activeUntil.get(row.symbol);if(active!=null&&t<active){skipped.activeSameSymbol++;continue;}accepted.push(Object.freeze({...row,persistenceCount:count,persistenceRuleApplied:true,oneActiveTradePerSymbol:true}));activeUntil.set(row.symbol,Date.parse(row.ratchet.outcomeAt));}
function summary(rows){const x=rows.map(r=>Number(r.ratchet.netReturnPct)),p=x.filter(v=>v>0).reduce((s,v)=>s+v,0),n=-x.filter(v=>v<0).reduce((s,v)=>s+v,0);return{signalCount:x.length,hitRate:x.length?x.filter(v=>v>0).length/x.length:null,netAverageReturnPct:x.length?x.reduce((s,v)=>s+v,0)/x.length:null,grossAverageReturnPct:x.length?rows.reduce((s,r)=>s+Number(r.ratchet.grossReturnPct),0)/x.length:null,profitFactor:n>0?p/n:(p>0?Infinity:null),averageHoldingBars:x.length?rows.reduce((s,r)=>s+Number(r.ratchet.barsHeld),0)/x.length:null};}
const result={phase:`57.p23.10b-fresh-persistence-holdout-shard-${SHARD_NUMBER}`,status:'FRESH_SYMBOL_HOLDOUT_MEASURED_ONCE',shardIndex:SHARD_INDEX,shardNumber:SHARD_NUMBER,symbols,preRegistration:P23_10B_FRESH_HOLDOUT_POLICY,source:{windowDays:58,rawBars:base.rawBars,sessionCount:base.sessionCount,adaptiveReference:base.adaptiveReference,nestedStateMachineStatus:nested.status,matchedOuterRows:matched.length},baselineMatchedRatchet:summary(matched),persistenceAccepted:summary(accepted),accepted:Object.freeze(accepted),skipped,integrity:{symbolUniverseFrozenBeforeOutcomeRetrieval:true,ruleFrozenBeforeOutcomeRetrieval:true,holdoutOutcomesUsedForRuleSelection:false,postSelectionOnHoldoutForbidden:true,matchedEntriesNestedOuterOos:true,oneActiveTradePerSymbol:true,sameSessionOnly:true,roundTripCostPct:.05,development30Excluded:true,freshSymbolHoldout:true},edgeClaimAllowed:false,recommendationAllowed:false,executionAllowed:false,brokerWriteAllowed:false,excelOrderWriteAllowed:false,rssOrderFunctionAllowed:false,liveTradingAllowed:false,paperTradingAllowed:false,automaticPromotionAllowed:false,productionUpdateAllowed:false,overnightHoldingAllowed:false,transmitted:false,safety:PHASE57_P23_10B_SAFETY};fs.mkdirSync('artifacts',{recursive:true});fs.writeFileSync(`artifacts/phase57-p23-10b-fresh-persistence-holdout-shard-${SHARD_NUMBER}.json`,JSON.stringify(result,null,2));console.log(JSON.stringify({shard:SHARD_NUMBER,symbols,matched:matched.length,baseline:result.baselineMatchedRatchet,accepted:accepted.length,persistence:result.persistenceAccepted,skipped},null,2));
