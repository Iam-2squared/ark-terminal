import fs from 'node:fs';
import {CHART_QUALITY_HOLDOUT_UNIVERSE} from './phase57-chart-quality-holdout-universe.js';
import {buildSessionAwareMultiTimeframePerception} from './phase57-chart-perception-session-aware.js';
import {classifyHumanStyleSetup} from './phase57-chart-perception-measurement.js';
import {scoreHumanStyleSetupQuality} from './phase57-chart-setup-quality.js';
import {deriveVisualChartReasoning} from './phase57-visual-chart-reasoning.js';
import {setupSpecificInvalidationLabel,P23_24_LABEL_POLICY} from './phase57-setup-specific-invalidation-labels.js';
import {probabilityStateFrom} from './phase57-setup-specific-invalidation-probability.js';
import {deltaState,dynamicDiagnostics,P23_29_POLICY} from './phase57-dynamic-state-deterioration.js';
const sleep=ms=>new Promise(r=>setTimeout(r,ms));const symbols=process.env.PHASE57_SYMBOLS?.split(',').map(x=>x.trim()).filter(Boolean)??CHART_QUALITY_HOLDOUT_UNIVERSE.slice(0,3);const minHistoryBars=Number(process.env.PHASE57_MIN_HISTORY_BARS??1600),stepBars=Number(process.env.PHASE57_STEP_BARS??3),maxContextBars=Number(process.env.PHASE57_MAX_CONTEXT_BARS??2600);const JST=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Tokyo',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'});function parts(ts){const p=Object.fromEntries(JST.formatToParts(new Date(ts)).map(x=>[x.type,x.value]));return{date:`${p.year}-${p.month}-${p.day}`,hm:`${p.hour}:${p.minute}`};}function norm(rows=[]){return rows.map(r=>({timestamp:new Date(r.timestamp).toISOString(),open:+r.open,high:+r.high,low:+r.low,close:+r.close,volume:+(r.volume??0)})).filter(r=>[r.open,r.high,r.low,r.close].every(Number.isFinite)&&r.high>=r.low).sort((a,b)=>a.timestamp.localeCompare(b.timestamp));}async function fetchJson(urls,s){let last;for(const u of urls)for(let a=1;a<=4;a++)try{const c=new AbortController(),t=setTimeout(()=>c.abort(),30000),res=await fetch(u,{headers:{'User-Agent':'Mozilla/5.0 ArkTerminalResearch/1.0',Accept:'application/json'},signal:c.signal});clearTimeout(t);if(!res.ok)throw Error(`${s} Yahoo HTTP ${res.status}`);return await res.json();}catch(e){last=e;if(a<4)await sleep(a*1000);}throw last;}function parseYahoo(j,s){const r=j?.chart?.result?.[0];if(!r)throw Error(`${s} missing Yahoo result`);const q=r.indicators?.quote?.[0]??{},out=[];for(let i=0;i<(r.timestamp??[]).length;i++){const ts=+r.timestamp[i]*1000,p=parts(ts);if(!((p.hm>='09:00'&&p.hm<'11:30')||(p.hm>='12:30'&&p.hm<'15:30')))continue;const v=[q.open?.[i],q.high?.[i],q.low?.[i],q.close?.[i]];if(v.some(x=>x==null||!Number.isFinite(+x)))continue;out.push({timestamp:new Date(ts).toISOString(),open:+q.open[i],high:+q.high[i],low:+q.low[i],close:+q.close[i],volume:+(q.volume?.[i]??0)});}return norm(out);}async function fetchBars(s){const end=Math.floor(Date.now()/1000),start=end-58*86400,q=`period1=${start}&period2=${end}&interval=5m&includePrePost=false`;return parseYahoo(await fetchJson([1,2].map(h=>`https://query${h}.finance.yahoo.com/v8/finance/chart/${encodeURIComponent(s)}?${q}`),s),s);}
const rows=[];for(const symbol of symbols){const bars=await fetchBars(symbol);await sleep(220);for(let i=Math.max(24,minHistoryBars);i<bars.length-8;i+=Math.max(1,stepBars)){const baseCtx=bars.slice(Math.max(0,i+1-maxContextBars),i+1),per=buildSessionAwareMultiTimeframePerception({bars5m:baseCtx}),setup=classifyHumanStyleSetup(per);if(![1,-1].includes(+setup.directionSign))continue;const q=scoreHumanStyleSetupQuality(per,setup);if(+q.score<.70)continue;const date=parts(bars[i].timestamp).date,direction=setup.directionSign===1?'UP':'DOWN',baseVisual=deriveVisualChartReasoning({symbol,bars5m:baseCtx,setupInfo:{setup:setup.setup,direction}}),baseState=probabilityStateFrom({visual:baseVisual,qualityScore:+q.score});for(const off of P23_29_POLICY.snapshotOffsets){const j=i+off;if(parts(bars[j].timestamp).date!==date)continue;const nowCtx=bars.slice(Math.max(0,j+1-maxContextBars),j+1),nowPer=buildSessionAwareMultiTimeframePerception({bars5m:nowCtx}),nowQ=scoreHumanStyleSetupQuality(nowPer,setup),nowVisual=deriveVisualChartReasoning({symbol,bars5m:nowCtx,setupInfo:{setup:setup.setup,direction}}),nowState=probabilityStateFrom({visual:nowVisual,qualityScore:+nowQ.score}),future=bars.slice(j+1,j+1+P23_24_LABEL_POLICY.horizonBars),entry=bars[j+1];if(!entry||future.length<P23_24_LABEL_POLICY.horizonBars||future.some(b=>parts(b.timestamp).date!==date))continue;const label=setupSpecificInvalidationLabel({setup:setup.setup,direction,entryPrice:entry.open,signalBar:bars[j],preSignalBars:nowCtx.slice(-8),futureBars:future});rows.push({symbol,setup:setup.setup,direction,sessionDate:date,timestamp:bars[j].timestamp,offsetBars:off,delta:deltaState(baseState,nowState),actual:label.invalidated?1:0,nextDirectionalReturnPct:label.nextDirectionalReturnPct});}}}
const result={phase:'57.p23.29-dynamic-state-deterioration',status:'DYNAMIC_STATE_DETERIORATION_COMPLETE',symbols,symbolCount:symbols.length,rowCount:rows.length,diagnostics:dynamicDiagnostics(rows),policy:P23_29_POLICY,methodology:{strongEntryFrozenQ4:true,setupSpecificP2324Label:true,dynamicSnapshots:true,noWinnerSelection:true,noExitPolicyChange:true,noThresholdSweep:true,noEntryRetuning:true,noSymbolFiltering:true,developmentDiagnostic:true,freshHoldoutConsumed:false},executionAllowed:false,brokerWriteAllowed:false,excelOrderWriteAllowed:false,rssOrderFunctionAllowed:false,liveTradingAllowed:false,paperTradingAllowed:false,automaticPromotionAllowed:false,productionUpdateAllowed:false,overnightHoldingAllowed:false,transmitted:false};for(const k of ['executionAllowed','brokerWriteAllowed','excelOrderWriteAllowed','rssOrderFunctionAllowed','liveTradingAllowed','paperTradingAllowed','automaticPromotionAllowed','productionUpdateAllowed','overnightHoldingAllowed','transmitted'])if(result[k]!==false)throw Error(`${k} must remain false`);fs.mkdirSync('artifacts',{recursive:true});fs.writeFileSync('artifacts/phase57-p23-29-dynamic-state-deterioration.json',JSON.stringify(result,null,2));console.log(JSON.stringify({status:result.status,rowCount:result.rowCount,diagnostics:result.diagnostics},null,2));
