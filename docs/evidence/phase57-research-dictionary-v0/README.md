# Research Dictionary v0

Lane H only. All estimates are RESEARCH_ONLY / HISTORICAL_RECONSTRUCTION / NOT_FOR_PRODUCTION / NOT_OOS_CONFIRMED / NOT_PIT_VERIFIED. The formal PIT Dictionary and prospective Lane P are unchanged.

Registry precommit: `4b421c28019095ed2924c8edb9338df01c2ec1b3`.73 scalar hypotheses,31 vocabulary atoms,6 descriptive composites. Composite counts are not additional significance tests. Half A24 sessions, embargo5, Half B23, initial burn-in5. Missing S may extend a symbol's effective burn-in. Previous10 true ranges require at least5 valid prior observations. No current day enters S or reference volume.

Existing57 Development dates only. Reuse encrypted input through the existing selective extractor; protected/REPORT19 payloads are not extracted or parsed. Input bytes must match the previous audit hashes. No new provider access. Historical code keys retain unresolved identity periods and are never model features.

Intraday eligibility uses reconciled daily/minute volume and value and a conservative absolute0.1JPY H/L tolerance. This is a reconstruction screen, not PIT proof. Missing minutes remain UNKNOWN. No bar OHLC is imputed. Only five fully observed consecutive minutes form a5m bar. Fixed response windows require complete consecutive closes and never cross lunch. Sparse observed HOD/LOD is explicitly retrospective day-summary information, never a contemporaneous feature.

CL is structurally unavailable in Half A before the2024-11-05 close extension; it cannot pass paired coverage. Pre-change15:00auction belongs to PM2, post-change15:30auction to CL; afternoon opening-auction volume cannot be isolated. Daily action flags exclude affected current/prior observations, but this cannot prove complete historical corporate-action provenance.

A-only OLS peer covariates: log trading value, log causal S, log price proxy, minute coverage. A eligibility, standardization and prior fit do not use Half B values. Normal-normal residual shrinkage uses prior strength20; rates use Beta posterior means and delta-method logit posterior SD. nEff corrects positive lag1 serial correlation and is capped by session count. Bar count is not sample size. Sparse conditional traits use episodes>=8 plus eligible days>=20 in each half.

Primary tests are A->B incremental Spearman reliability. Fixed500 moving-session-block bootstrap draws per half, length3, all73 hypotheses in BH FDR q<=0.10. Bootstrap refits A peer coefficients; missing bootstrap samples are not imputed. Random split, reverse direction, market-relative medians, liquidity strata and top3-session exclusion are diagnostics/gates exactly specified before measurement. Redundant accepted scalars are dropped in registry order; no post-hoc factors or replacement traits.

Profiles refer to A-only estimates as of the first embargo session. B estimates are evaluator evidence. Insufficient symbol/trait entries are retained, with no own-deviation estimate promoted; shrink weight is zero and a prior is unavailable when A cannot support a peer fit. This is not a production feature loader: research artifacts are rejected even if callers attempt to relabel them.

The new workflow measures twice, compares manifests, runs contract tests and the complete offline regression suite, then preserves evidence on the research branch only. Runtime receipts are outside the deterministic manifest; all scientific outputs are inside. Numerical sample logs are not independent human chart validation; Human Semantics Audit remains NOT_APPLICABLE.

No Entry/EXIT P&L, Chart Context learning, Selector change, Capital change, Fresh/OOS claim, trading, automatic promotion or main merge.
