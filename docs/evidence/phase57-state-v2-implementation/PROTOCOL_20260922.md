# State v2 implementation / generation protocol — 2026-09-22 JST

Scope: frozen State v2 implementation and Development reference acceptance only.
No recognition fitting, Signal, Entry, EXIT, portfolio, protected evaluation or orders.
Source baseline: 38c789c39a30618b449435d1fc8ec7410a1b5dc6.
Initial dedicated bootstrap: 44b1e55a0ad8721df8bec4013e60e735ac698b74.

## Source pins and original-byte recovery
The immutable mechanical-v1 engine and G admission are reused, not edited. Restore
four exact authorized workflow artifacts; verify ZIP digest, inner manifest and
all admitted member hashes. G is the identity authority for the 2,155 allowlist;
the old 77,214 reference is the key-level baseline, not NOW input.

The original spec and hardening Library files were recovered and match the frozen
manifest SHA256 exactly. GitHub stored copies at freeze/handoff have one fewer
terminal LF byte. Preserve these stored copies and both failed bootstrap runs.
Only the two exact old hashes may have exactly one LF appended to a separate
verified effective copy. The result MUST equal the original frozen hash. Do not
change the manifest, normalize arbitrary content, or weaken a hash gate.

## Implementation boundaries
NOW and Future are different modules, validators, files and hashes. NOW accepts
only a prefix plus fixed prior-session / exact-lag context. Per-primitive readers
reject event times and knownAt after cutoff before releasing bars. Missing knownAt
is null with HISTORICAL_CLOSED_RECONSTRUCTION, never fabricated from bar end.
Only identity is joined; selectorPrice is used in the coverage report, never State.

The inherited snapshot's State prerequisites, not Direction's latest5 requirements,
control Phase/Structure. An active inherited Structure is not downgraded by local
pivot count. All rule-critical prices remain Fraction/exact. Future uses unchanged
reference_at, effectiveAt<=t, confirmedAt<=t+10, and independent censor flags.
Structure-resolution delay is reported from the admitted active-Structure witness
confirmation time (zero if already confirmed). Phase has its own adjudicated axis.
This bookkeeping interpretation is exposed for independent implementation review.

## Replay
Generate all 77,214 keys, including missing bars. Run fresh checkpoint evaluation
again with reversed opportunity/checkpoint order and different worker count.
On replay, mutate values strictly after t before NOW routing; compare its newly
computed canonical bytes to the saved first run for all rows. Also remove the suffix
and verify the routed prefix is identical. Future is computed from the original
H-bounded copy, never from the deliberately mutated NOW-isolation probe.

## Transition and reporting
Compare keys (opportunity, session, symbol, selectorAt, checkpointAsOf, elapsed grid)
to every old row. Compare v2 Future values to old Future values; expected numerical
changes=0. Compare NOW to old Future separately: a difference must have a delayed
pivot witness, not a vocabulary/numeric-rule change. Closed change reason codes,
status cross-tabs, concentrations and coverage are descriptive only.

## Acceptance honesty
Apply the union of handoff and original hardening A1-A12 (including provenance,
independent implementation, fresh replay, explanation and Safety). The second
minimal implementation independently checks the semantic projection, not every
numeric primitive. Do not present that as an external reviewer or full independent
engine reimplementation. A8's inherited as-of/price-basis limitations remain open
where independent provenance is unavailable; report the exact extent, not a fake
fully-PIT PASS. All source/working code hashes and test failures are preserved.
Only all required gates fully PASS permits an Accepted receipt. Otherwise write
a NOT_ACCEPTED disposition and prepare a narrow independent review package.

## Audit-predicate correction after failed first run (16:xx JST)
The first generation correctly failed closed on UNEXPLAINED_CAUSAL_STATE_CHANGE.
The audit had incorrectly assumed all NOW-versus-ORACLE differences require a
pivot newly confirmed after t. A Development counterexample had late count=0:
NOW Phase=[], ORACLE Phase=[RESTRUCTURING]. Replaying the same prefix and same
confirmed pivots on the frozen engine's effective-time timeline reproduces the
ORACLE result exactly. This is inherited historical replay semantics, not a new
numeric rule or future-bar leak. The revised transition audit decomposes the
change through a prefix-only retrospective counterfactual; both stages must be
recomputable. Preserve the failed run and its old runner. Do not alter either
NOW or Future numeric labels to satisfy the audit.
