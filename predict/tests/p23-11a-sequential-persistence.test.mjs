import assert from 'node:assert/strict';import {PHASE57_P23_11A_SAFETY,P23_11A_RULE,applySequentialPersistenceScheduler,summarizeSequentialTrades} from '../daytrade/phase57-sequential-persistence-scheduler.js';for(const k of ['executionAllowed','brokerWriteAllowed','excelOrderWriteAllowed','rssOrderFunctionAllowed','liveTradingAllowed','paperTradingAllowed','automaticPromotionAllowed','productionUpdateAllowed','overnightHoldingAllowed'])assert.equal(PHASE57_P23_11A_SAFETY[k],false);assert.equal(P23_11A_RULE.requiredSameDirectionSignals,3);assert.equal(P23_11A_RULE.lookbackMinutes,15);function r(min,dir='LONG',outMin=min+10,net=.2){return{symbol:'X',sessionDate:'2026-08-01',featureCutoff:new Date(Date.parse('2026-08-01T00:00:00Z')+min*60000).toISOString(),direction:dir,ratchet:{outcomeAt:new Date(Date.parse('2026-08-01T00:00:00Z')+outMin*60000).toISOString(),netReturnPct:net}};}const rows=[r(0),r(5),r(10),r(12),r(20),r(25)];const out=applySequentialPersistenceScheduler(rows);assert.equal(out.accepted.length,2);assert.equal(out.accepted[0].persistenceCount,3);assert.equal(out.integrity.oneActiveTradePerSymbol,true);const s=summarizeSequentialTrades(out.accepted);assert.equal(s.signalCount,2);assert.ok(Number.isFinite(s.netAverageReturnPct));console.log('P23.11A sequential persistence tests passed');