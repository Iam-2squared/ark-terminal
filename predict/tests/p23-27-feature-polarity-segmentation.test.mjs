import assert from 'node:assert/strict';import {P23_27_POLICY,featureDiagnostic,segmentDiagnostics} from '../daytrade/phase57-feature-polarity-segmentation.js';
assert.equal(P23_27_POLICY.thresholdSearchAllowed,false);assert.equal(P23_27_POLICY.selectionAllowed,false);assert.equal(P23_27_POLICY.exitPolicyChangeAllowed,false);
const rows=[{setup:'A',direction:'UP',actual:1,nextDirectionalReturnPct:-.5,state:{structureCoherence:.9,directionalPressure:.3,wickControl:.3,extensionHealth:.3,spaceToObstacle:.2,setupGeometryFit:.5,qualityScore:.6}},{setup:'A',direction:'UP',actual:0,nextDirectionalReturnPct:.4,state:{structureCoherence:.2,directionalPressure:.7,wickControl:.7,extensionHealth:.7,spaceToObstacle:.8,setupGeometryFit:.9,qualityScore:.9}},{setup:'A',direction:'DOWN',actual:1,nextDirectionalReturnPct:-.4,state:{structureCoherence:.1,directionalPressure:.8,wickControl:.8,extensionHealth:.8,spaceToObstacle:.9,setupGeometryFit:.8,qualityScore:.7}},{setup:'A',direction:'DOWN',actual:0,nextDirectionalReturnPct:.3,state:{structureCoherence:.8,directionalPressure:.4,wickControl:.4,extensionHealth:.4,spaceToObstacle:.3,setupGeometryFit:.9,qualityScore:.9}}];
const d=featureDiagnostic(rows.filter(x=>x.direction==='UP'),'structureCoherence');assert.equal(d.riskOrderingAuc,1);const s=segmentDiagnostics(rows);assert.equal(s.overall.n,4);assert.equal(s.bySetupDirection['A__UP'].n,2);assert.ok(s.polarityConflicts.some(x=>x.feature==='structureCoherence'));console.log('p23.27 polarity segmentation invariants ok');
