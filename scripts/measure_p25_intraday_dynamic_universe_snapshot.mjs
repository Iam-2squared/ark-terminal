import fs from 'node:fs';import {buildIntradayDynamicUniverseTimeline} from '../predict/daytrade/phase57-p25-intraday-dynamic-universe.js';
const arg=(n,f=null)=>{const i=process.argv.indexOf(n);return i>=0&&i+1<process.argv.length?process.argv[i+1]:f};const input=arg('--input');const output=arg('--output','tmp/p25-intraday-dynamic-universe-measurement.json');if(!input)throw new Error('--input required');const payload=JSON.parse(fs.readFileSync(input,'utf8'));const entries=Array.isArray(payload)?payload:(payload.entries??[]);const asOf=payload?.meta?.observedAt??new Date().toISOString();const started=performance.now();const timeline=buildIntradayDynamicUniverseTimeline({snapshots:[{asOf,entries}]});const elapsedMs=performance.now()-started;const point=timeline.points[0];const result={schemaVersion:1,phase:'57.p25.intraday-dynamic-universe-marketwide-measurement',status:'MARKETWIDE_DYNAMIC_5M_MEASUREMENT_READY',observedAt:asOf,inputSymbols:entries.length,selectedSymbols:point?.rawUniverse?.length??0,allocationEligibleSymbols:point?.allocationEligibleUniverse?.length??0,selectorCalls:timeline.diagnostics.selectorCalls,prescreenedRows:timeline.diagnostics.prescreenedRows,selectorElapsedMs:Number(elapsedMs.toFixed(3)),methodology:timeline.methodology,policy:timeline.policy,safety:timeline.safety,selected:(point?.rawUniverse??[]).map(x=>({symbol:x.symbol,sector:x.sector,opportunityScore:x.opportunityScore,turnoverYen:x.turnoverYen}))};fs.writeFileSync(output,JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify(result,null,2));if(result.inputSymbols<3000||result.selectedSymbols<20)process.exit(1);
