import fs from 'node:fs';import path from 'node:path';
const root=process.argv[2]??'artifacts/p23-42-shards';const files=[];for(const d of fs.readdirSync(root)){const p=path.join(root,d);if(fs.statSync(p).isDirectory())for(const f of fs.readdirSync(p))if(f.endsWith('.json'))files.push(path.join(p,f));else{}else if(d.endsWith('.json'))files.push(p);}if(!files.length)throw Error('no P23.42 shard artifacts');const shards=files.map(f=>JSON.parse(fs.readFileSync(f,'utf8')));const window0=JSON.stringify(shards[0].frozenWindow);if(shards.some(s=>JSON.stringify(s.frozenWindow)!==window0))throw Error('frozen window mismatch');const names=['p2330','p2336','p2337','p2338','p2340','p2341','p2343','p2344','p2345','p2348','p2349','p2350','p2351'];const dirs=['UP','DOWN'];const weighted=(arr,getN,getV)=>{const z=arr.map(x=>({n:+getN(x)||0,v:getV(x)})).filter(x=>x.n>0&&Number.isFinite(+x.v));const n=z.reduce((s,x)=>s+x.n,0);return n?z.reduce((s,x)=>s+x.n*(+x.v),0)/n:null;};const model={};for(const name of names){const ss=shards.map(s=>s.models[name]);if(ss.some(x=>!x))throw Error(`${name} missing from shard`);model[name]={summary:{n:ss.reduce((a,x)=>a+(x.summary.n??0),0),weightedShardOrderingAuc:weighted(ss,x=>x.summary.n,x=>x.summary.orderingAuc),weightedMeanDirectionalReturnPct:weighted(ss,x=>x.summary.n,x=>x.summary.meanDirectionalReturnPct)},byDirection:{}};for(const d of dirs){model[name].byDirection[d]={n:ss.reduce((a,x)=>a+(x.byDirection?.[d]?.n??0),0),weightedShardOrderingAuc:weighted(ss,x=>x.byDirection?.[d]?.n,x=>x.byDirection?.[d]?.orderingAuc),weightedMeanDirectionalReturnPct:weighted(ss,x=>x.byDirection?.[d]?.n,x=>x.byDirection?.[d]?.meanDirectionalReturnPct)};}}
const result={phase:'57.p23.42-frozen-development-benchmark-aggregate',status:'FROZEN_DEVELOPMENT_BENCHMARK_AGGREGATE_COMPLETE',shardCount:shards.length,frozenWindow:shards[0].frozenWindow,rowCount:shards.reduce((a,s)=>a+s.rowCount,0),scoredCount:shards.reduce((a,s)=>a+s.scoredCount,0),models:model,dataFingerprints:Object.assign({},...shards.map(s=>s.dataFingerprints)),methodology:{sameRowsWithinShard:true,fixedAbsoluteWindow:true,developmentBenchmarkOnly:true,noThresholdSweep:true,noWinnerSelectionDuringScoring:true,freshHoldoutConsumed:false},executionAllowed:false,brokerWriteAllowed:false,excelOrderWriteAllowed:false,rssOrderFunctionAllowed:false,liveTradingAllowed:false,paperTradingAllowed:false,automaticPromotionAllowed:false,productionUpdateAllowed:false,transmitted:false};fs.mkdirSync('artifacts',{recursive:true});fs.writeFileSync('artifacts/phase57-p23-42-frozen-development-benchmark-aggregate.json',JSON.stringify(result,null,2));console.log(JSON.stringify(result,null,2));