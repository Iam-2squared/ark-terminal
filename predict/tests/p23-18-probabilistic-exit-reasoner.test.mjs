import assert from 'node:assert/strict';import {buildExitState,estimateExitEvidence,P23_18_EXIT_EVIDENCE_POLICY} from '../daytrade/phase57-probabilistic-exit-reasoner.js';
assert.equal(P23_18_EXIT_EVIDENCE_POLICY.thresholdSearchAllowed,false);assert.equal(P23_18_EXIT_EVIDENCE_POLICY.outcomeTuningAllowed,false);assert.equal(P23_18_EXIT_EVIDENCE_POLICY.requirePriorSession,true);
const bars=[{open:100,high:101,low:99.5,close:100.5},{open:100.5,high:101.2,low:100,close:101},{open:101,high:101.1,low:100.2,close:100.4}];const state=buildExitState({entryPrice:100,direction:'UP',bars});assert.ok(state&&Number.isFinite(state.givebackPctPoints));
const pool=Array.from({length:40},(_,i)=>({setup:'BREAKOUT_CONTINUATION_UP',direction:'UP',sessionDate:'2026-01-01',fullyRealizedAt:`2026-01-01T0${i%9}:00:00.000Z`,state,nextDirectionalReturnPct:i<30?-0.2:0.1}));const ev=estimateExitEvidence({setup:'BREAKOUT_CONTINUATION_UP',direction:'UP',sessionDate:'2026-01-02',timestamp:'2026-01-02T05:00:00.000Z',state},pool);assert.equal(ev.ready,true);assert.ok(ev.pReversal>0.5);assert.ok(ev.lowerReversal90>0.5);assert.equal(ev.decision,'EXIT');console.log('P23.18 probability invariants OK');