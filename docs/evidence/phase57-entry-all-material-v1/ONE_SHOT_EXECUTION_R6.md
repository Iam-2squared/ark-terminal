# All-Material Entry R1 — one-shot execution record R6

Date: 2026-09-25 JST
Observed branch HEAD before this record: 7d15b4d47b70a883aebce4f3df2a88ad86ba414b.
Scope: user-requested one-time research execution; scheduled automation remains stopped.

## User instruction superseding transition interpretation

Complete the precommitted R1 finite nested evaluation once, then stop further Entry optimization and report Latest R1 vs ONE_MINUTE vs Immediate using the same canonical A-J scorecard. Report the original mean EntryPosition <25% and <15% thresholds without rewriting past outcomes. A failure of the 25% location target alone no longer prohibits subsequent research-only EXIT integration. This is not a statistical PASS, generalization claim, or automatic production promotion. Integrity and safety requirements remain mandatory.

## Execution prerequisites, not performance results

The committed coverage audit still assumes 59 opportunity-bearing sessions and 2,155 T0 rows. Previous pre-fit observations report 58 opportunity-bearing sessions, 1,954 T0 rows and 201 structural T+1 starts. Validate these observations against pinned sources, preserve the 2,155 denominator and repair the accounting before fitting. Missing checkpoints must never be fabricated.

Before any model performance run, persist the corrected coverage evidence and exact admitted-feature/missing/split manifests; verify the immutable ONE_MINUTE reconstruction against the accepted current-cohort comparator. The prior-proposed inner splitter is not silently accepted by this record: its exact train/purge/validation identities and lineage still require validation and a separate pre-fit freeze.

The finite R1 search remains M1 L2 logistic C=1, M2 L1 logistic C=1, M3 compact gradient boosting depth=3, learning_rate=.05, iterations=100; H in {5,10,20,30}; probability in {.40,.50,.60}. No outcome-driven additions. No learned results are asserted in this record.

## Boundaries

Frozen Selector, original State-v3 and six timing Signals remain immutable. Existing Development sources only; provider requests=0; no new Fresh/OOS/Common Holdout/REPORT19/Validation/Prospective access. No main merge. executionAllowed=false; brokerWriteAllowed=false; excelOrderWriteAllowed=false; rssOrderFunctionAllowed=false; liveTradingAllowed=false; paperTradingAllowed=false; automaticPromotionAllowed=false; productionUpdateAllowed=false; transmitted=false.
