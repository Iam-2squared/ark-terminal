# NEW LONG EXIT: bounded loss-state separation study v1

Date: 2026-09-18 JST. Source HEAD: e6b98886a35524c98ae4b6ec5913210de9525c80.
User continuous-research directive supersedes the previous audit-only stop;
Candidate C remains KILL. Historical Development is already outcome-exposed.
This is a bounded descriptive screen, not independent validation or model fit.
Publish this protocol before measuring any new state. Do not revise gates after
seeing results, choose thresholds from distributions, or select a best time.

## Immutable sources and population

Use saved conditional-transfer ledger (3,284 opportunities), saved 5m paths,
frozen Entry parity ledger, Candidate A policy, Path Study ledger/report and
Candidate C KILL decision. Verify all upstream content hashes and 2,743 anchor
identities. Study all opportunities for observation coverage; paired EXIT
statistics use the exact Fixed12-comparable INITIAL 1,072 / DIP 397 identities.
Reuse saved Fixed12 outcomes; do not rerun old research workflows or Entry.
Candidate A is evaluated as the necessary fixed paired comparator only.
No other market data, Fresh/OOS, provider, fit, 1m or runtime access.

## Measurements fixed before new outcome inspection

At Entry +5/+10/+15 CLOCK minutes, completed bars only, with no lunch/session
crossing or missing-bar interpolation. Decision features receive only an OHLC
prefix. Future values and all labels are separate evaluator-only payloads.
Missing/zero-range features remain null, never negative/zero imputation.
Features: Entry displacement; close location; upper/lower wick fractions;
high-to-close rejection; low-to-close recovery; range; range ratio;
range-normalized displacement; new-low count and extension; last close change;
change acceleration; normalized adverse step; normalized adverse acceleration;
path efficiency; bounce from minimum close; reclaim occurrence; recovery slope.
Report distributions and fixed-direction rank AUC of deep-failure versus
recovery labels (ties half credit); no cut-point search or fitted classifier.

At each time among current negative-close, Candidate-A-still-held positions:
recovery3/5 = first later +3/+5 HIGH with no earlier touch; deep failure =
Candidate A net <= -5%; continued deterioration = later LOW at least 2pp below
current CLOSE. Labels may overlap; report overlap, not a fabricated binary
partition. Latency: report next-OPEN-to-A net rescue proxy and already incurred
loss for these labels at each time, including matched t5/t10/t15 identities.
These conditional oracle summaries are not executable policy performance.
Report INITIAL/DIP, fixed 19-session chronological blocks, frequency-top3
exclusion, symbol/session counts, concentration and full underlying-minute
coverage sensitivity. No symbol or time-of-day decisions.

## Exactly three structural hypothesis screens, no EXIT candidates yet

All are assessed at the first three completed contiguous bars (+15 minutes).
This fixed three-observation window is needed to observe failure after attempted
recovery or acceleration; +5/+10 are diagnostics, not alternative exit times.
Let O/H/L/C be Entry-relative values, R=H-L, LW=(min(O,C)-L)/R.
All screens require Candidate A still held and C3<0.

1. RECLAIM_REJECTION: C1<0, C2>=0, C3<0; H3<H2, L3<L2;
   C3 below the midpoint of bar3's range. This observes an actual CLOSE reclaim
   followed by rejection, unlike the killed no-reclaim/lower-close rules.
2. FAILED_BOUNCE: C1<C2<0 and C1<0; C3<C1, H3<H2, L3<L1.
   This observes an actual improving CLOSE followed by complete bounce failure,
   not merely two decreasing CLOSEs or a timed no-reclaim window.
3. NORMALIZED_ACCELERATION: all three CLOSEs<0; L3<L2<L1;
   R3>R2>0; LW3<LW2; (C2-C3)/R2 > (C1-C2)/R1 > 0.
   This combines volatility-normalized acceleration with range expansion and
   declining wick recovery. No absolute stop or fitted volatility threshold.

Screen eligibility (ALL required; null = insufficient evidence, never PASS):
- at least 30 selected total, at least 10 in EACH cohort, >=3 blocks represented;
- selected deep failures >=10 total (avoid a handful of lucky cases);
- later >=2pp deterioration rate >=1.5x current-negative baseline in EACH cohort;
- recovery3 and recovery5 rates <=0.5x current-negative baseline in EACH cohort;
- selected deterioration rate exceeds baseline in >=3/4 pooled chronological
  blocks, and the cohort enrichment/recovery conditions survive frequency-top3
  exclusion (frequency and deterministic symbol tie-break only).
These are conservative entry-to-research gates, not scientific proof of
nonseparability. Report every failed gate and every screen, never only a winner.

## Bounded continuation and stop

Maximum three architecture screens, no combination search, routing rescue,
time optimization, retuning or fourth screen on these exposed outcomes.
If a screen qualifies, take it in the numbered order, publish a separate
candidate contract BEFORE implementing/measuring its EXIT. Candidate A profit
protection remains unchanged and takes priority; exact Fixed12 fallback;
next regular OPEN after signal; .05pp cost; no re-entry. One candidate at a time.
Candidate gates must jointly address mean/PF/tail, strict +3/+5 preservation,
deep-loss rescue, both cohorts, blocks, concentration and causal-ledger audit.
If no screen qualifies, combine these new findings with the already killed
loss-defense architectures v0/v1/v2/v3 and preserve Candidate A as fallback:
EXIT_ARCHITECTURE_DEVELOPMENT_LIMIT_REACHED. This means the bounded authorized
research failed to establish a stable separator, NOT that every possible 5m
architecture is mathematically impossible. Do not invent a final strong EXIT.
Any qualifying candidate that fails remains KILL; no threshold repairs.

## Audit and safety

Strict preservation: exit-at-OPEN counts same-bar HIGH only if OPEN itself meets
the milestone; Fixed12 CLOSE retains completed-bar semantics. Record both the
strict A baseline and its historical figures without editing old evidence.
Prefix/suffix perturbation, source pins, identity join, deterministic bytes,
missing/boundary tests, exact cost and saved-Fixed12 ledger checks required.
New outputs refuse existing destinations. Network disabled during research.
All nine execution/write/trading/promotion flags false; transmitted=false.
LONG cash equity only; no SHORT, margin, leverage, Capital, Portfolio, main merge.
Fresh/OOS stays sealed even if a strong candidate is frozen.
