import assert from 'node:assert/strict';import {estimateStructuralSurvival,structuralStateFrom,P23_21_POLICY} from '../daytrade/phase57-structural-survival-exit.js';
const visual={components:{structureCoherence:.7,directionalPressure:.6,wickControl:.8,extensionHealth:.7,spaceToObstacle:.6,setupGeometryFit:.8}};const q=structuralStateFrom({visual,currentReturnPct:.4,givebackPctPoints:.1});assert.equal(q.structureCoherence,.7);const pool=[];for(let i=0;i<25;i++)pool.push({setup:'BREAKOUT_CONTINUATION_UP',direction:'UP',timestamp:`2026-01-${String(i+1).padStart(2,'0')}T00:00:00.000Z`,fullyRealizedAt:`2026-01-${String(i+1).padStart(2,'0')}T00:15:00.000Z`,structuralState:q,nextDirectionalReturnPct:i<20?-.2:.2,survived:i>=20});const ev=estimateStructuralSurvival({setup:'BREAKOUT_CONTINUATION_UP',direction:'UP',timestamp:'2026-02-01T00:00:00.000Z',structuralState:q},pool);assert.equal(ev.ready,true);assert.ok(ev.pFailure>.5);assert.equal(P23_21_POLICY.thresholdSearchAllowed,false);console.log('P23.21 invariants OK');