# Phase57 All-Material Entry v1 — PROGRESS R3

2026-09-24 JST

## Completed in this increment

- Re-read latest PR #587 HEAD and All-Material R0/R1/R2 contracts before modification.
- Confirmed exact committed input paths for the 2,155 opportunity ledger, Immediate, Entry v1 and
  original State-v3 records.
- Re-confirmed the accepted ONE_MINUTE authority from the bounded-pass final report and its source
  run/artifact/file hashes.
- Traced a real prefix-causal producer for four additional information features and kept model
  admission closed pending exact 2,155 join/coverage.
- Added an additive evaluator that extends Capture to +4 while fail-closed asserting exact
  `+1/+2/+3/+5` canonical parity.
- Added +1..+5 threshold panels, future-MFE range buckets, Low->Entry absolute bps, remaining-upside
  bps, <=10/15/25/50 rates, State split and concentration diagnostics.
- Added focused tests and a dedicated offline CI replay for Immediate / Entry v1 / original State-v3
  / accepted ONE_MINUTE, including two-run deterministic replay.
- No new Entry decision, model fit, threshold selection or ABSTAIN selection was performed.

## Gate status

| Gate | Status |
|---|---|
| All-Material protocol frozen before performance | PASS |
| evaluator canonical Capture preservation | IMPLEMENTED / CI REQUIRED |
| accepted ONE_MINUTE identity | PINNED / CI REVERIFY REQUIRED |
| causal full-2,155 feature matrix | NOT YET COMPLETE |
| nested/OOF learned Entry | NOT YET RUN |
| mean EntryPosition `<25%` | NOT YET EVALUATED FOR NEW CANDIDATE |
| EXIT transition | LOCKED |

The new evaluator may re-display immutable baseline metrics, but that is not a new performance trial.

## Next executable dependency

1. finish conditional producer/knownAt/coverage joins into a causal full-2,155 feature matrix;
2. freeze exact admitted-column manifest and missing semantics;
3. implement the already-precommitted finite nested/OOF selection protocol;
4. freeze selected integrated candidate before whole-2,155 replay;
5. apply `<25%` + Fill/Capture/coverage/causality/common-case/concentration gates.

EXIT remains prohibited until Entry passes.
